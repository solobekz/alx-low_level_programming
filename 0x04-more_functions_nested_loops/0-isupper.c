#include "main.h"

/**
 * _isupper - checks for uppercase char
 * Return: 1 if c is uppercase and 0 if otherwise
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		Return (1);
	}
	else
	{
		Return (0);
	}
}
